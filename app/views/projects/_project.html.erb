
<h2><%= project.title %></h2>
<p>Uploaded on: <%= project.updated_at.to_date %> </p>

<%= image_tag project.title_page_img %>

<%= render project.content_partial_view_name %>

<h3>Comments</h3>
<hr>

<%= render 'comments/comment_form', comment: project.comments.build %>

<%= render 'comments/comments', comments: project.comments %>